{
  "name": "mhed89-github-io",
  "version": "1.0.0",
  "description": "Intern mental logg exponerad utan garantier. Ingen publik förväntad.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npx prettier --write . && node scripts/copy-assets.js && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  },
  "dependencies": {
    "marked": "^11.1.1",
    "mermaid": "^10.6.1"
  }
}
